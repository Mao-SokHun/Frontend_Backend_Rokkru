# Copy to .env.docker and adjust for local Docker runs:
#   cp .env.docker.example .env.docker
#   docker compose --env-file .env.docker up --build

POSTGRES_USER=rokkru
POSTGRES_PASSWORD=rokkru
POSTGRES_DB=rokkru
POSTGRES_PORT=5432

BACKEND_PORT=3000
FRONTEND_PORT=80

CORS_ORIGIN=http://localhost,http://localhost:80,http://localhost:5173
FRONTEND_URL=http://localhost
VITE_API_URL=/api

JWT_SECRET=change-me-in-production
JWT_REFRESH_SECRET=change-me-in-production-too
LOGINLIMIT=100
REGISTERLIMIT=50

# Optional integrations
EMAIL_USER=
EMAIL_PASS=
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_CURRENCY=usd
